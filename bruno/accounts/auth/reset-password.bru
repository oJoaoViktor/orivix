meta {
  name: Reset Password
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/auth/reset-password/
  body: json
  auth: none
}

body:json {
  {
    "uid": "{{uid}}",
    "token": "{{resetToken}}",
    "new_password": "novaSenha@456"
  }
}
